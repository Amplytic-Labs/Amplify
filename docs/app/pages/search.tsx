// Search API placeholder - in production, use source.apiSearch() with a proper server
// For SPA mode, we use client-side search (FlexSearch or Orama)
export default function SearchPage() {
  return null;
}
